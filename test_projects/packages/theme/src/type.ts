type ThemeName = "default" | "compact";
type ColorScheme = "normal" | "light" | "dark" | "light dark" | "dark light";
type ColorSchemeMode = "light" | "dark";

export type { ThemeName, ColorScheme, ColorSchemeMode };
